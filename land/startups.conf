#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = udiskie & pipewire
exec-once = code & firefox & discord
exec-once = hyprpaper
exec-once = dunst 
exec-once = udiskie
exec-once = hypridle
exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = wl-paste --type text --watch cliphist store #Stores only text data # cliphist
exec-once = wl-paste --type image --watch cliphist store #Stores only image data # cliphist
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # screen sharing stuff